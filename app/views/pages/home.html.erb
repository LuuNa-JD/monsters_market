<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Monster Market</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>
</head>
<body>
  <div class="container-home mt-5">
    <div class="row mt-5 pt-5 justify-content-center">
      <p class="slogan-home text-center animated-slogan">Vivez halloween autrement !</p>
      <div class="carousel-container">
        <div id="monsterCarousel" class="carousel slide" data-bs-ride="carousel" data-bs-touch="false" data-bs-interval="3000">
          <div class="carousel-inner">
            <% @monsters.each_with_index do |monster, index| %>
              <div class="carousel-item <%= 'active' if index.zero? %>">
                <img src="<%= cl_image_path monster.photo.key, width: 300, height: 200, crop: :thumb, quality: "auto:best", fetch_format: :auto %>" alt="<%= monster.name %>" class="card-img img-fluid d-block w-100">
                <div class="carousel-caption d-none d-md-block">
                  <h5 class="card-title"><%= monster.name %></h5>
                  <p class="card-price"><%= monster.price %>$ par semaine</p>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
    <div class="text-center mt-5">
      <a href="/monsters" class="btn-start-adventure">Commencez votre aventure</a>
    </div>
  </div>
</body>
</html>
